# ==============================================
# IBM Hackathon - Traffic Congestion Analysis
# ==============================================
# Note: LangFlow is used via Astra DataStax (hosted)
# No need to install langflow or langchain locally
# ==============================================

# IBM watsonx SDK (for custom scripts if needed)
ibm-watsonx-ai>=0.2.0

# Data Processing (Required for dataset handling)
pandas>=2.0.0
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
python-dateutil>=2.8.0

# Development & Testing (Optional)
pytest>=7.4.0
black>=23.0.0
